// Print the length of the array and the last item using index.

const fruits = ["banana", "mango", "orange", "grapes"];

console.log("Length:", fruits.length);

console.log("Last item:", fruits[fruits.length - 1]);


/* Output ---------------
Length: 4
Last item: grapes
*/

// fruits[fruits.length - 1] → accesses the last element (because index starts at 0).