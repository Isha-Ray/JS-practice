// Check if a user is either admin or super-admin using OR (||).

let user = "super admin";

if(user === "admin" || user === "super admin"){
    console.log("Access granted")
} else {
    console.log("Access denied")
}

/* Output ------
    Access granted

    || mtlb "OR" hota h jisme dono me se koi ek condition true ho toh wo run kar jayega. 

    */