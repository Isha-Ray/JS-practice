// Use .map() to return a new array with squares of numbers [1, 2, 3, 4].

let nums = [1, 2, 3, 4];
let squares = nums.map((n) => n * n);
console.log(squares);

// Output ----> [1, 4, 9, 16]

//map() – Transform each element and return a new array