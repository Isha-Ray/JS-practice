// Use .filter() to return only numbers greater than 10 from [5, 12, 18, 3].

// .filter() --- Return only elements that match a condition.

let nums = [5, 12, 18, 3];
let greaterNums = nums.filter((n) => n > 10);
console.log(greaterNums);

// Output --> [ 12, 18 ]